{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"python manage.py collectstatic --noinput && python manage.py migrate --noinput && gunicorn usar_backend.wsgi:application --bind 0.0.0.0:$PORT --workers 2\"",
    "healthcheckPath": "/api/v1/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
